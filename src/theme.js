import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#333',
    },
    background: {
      primary: '#04043f',
      light: '',
      dark: '',
      lightGrey: '#F2F4F6',
    },
    text: {
      primary: '#333',
    },
  },
})

export default theme
